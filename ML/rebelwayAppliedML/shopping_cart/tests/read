这是Pytest模块，只有命名格式以test开头的才会被检测到。

需要注意的是测试用例 test_cart.py 中使用了相对路径 ./tests/test_database.json 来加载数据库文件。

这意味着只能在shopping_cart目录执行 pytest时，

数据库相对路径才会正确解释为shopping_cart/tests/test_database.json





##########
该环境依赖jax.yml文件
执行pytest时，需要额外依赖pytest库
